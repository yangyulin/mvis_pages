---
title: Examples
description: Complete calibration runs on public and in-house datasets — pick one that matches your rig.
pageType: reference
wide: true
---

import ExampleGrid from '../../../components/ExampleGrid.astro';

Complete runs, each on a released dataset: the configuration used, the result
produced, and — where a reference calibration exists — how far the two differ.
Pick the one whose rig looks most like yours.

<ExampleGrid examples={[
  {
    title: 'TUM VI',
    href: '/examples/tum-vi/',
    sensors: 'Stereo 1024² fisheye + BMI160 @ 200 Hz',
    format: 'ROS1 bag',
    shows: 'All six distortion models on identical data, vs a published Kalibr reference',
  },
  {
    title: 'Monado',
    href: '/examples/monado/',
    sensors: 'Valve Index — stereo fisheye + IMU',
    format: 'EuRoC mav0',
    shows: 'Round-trip PASS and t_ga, vs the Monado reference baseline',
  },
  {
    title: 'Looper SA16',
    href: '/examples/looper-sa16/',
    sensors: '2–3 cameras (one rolling shutter) + IMU @ 400 Hz',
    format: 'Custom → EuRoC mav0',
    shows: 'Four recording campaigns: rolling shutter, IMU oversampling, hardware sync',
  },
]} />

## How to read these pages

Every example follows the same structure, so two of them can be compared
directly:

1. **The rig** — sensors, rates, and what makes the dataset interesting.
2. **Getting the data** — download and any preparation the bag needs.
3. **Configuration** — the launch file, in full.
4. **Running it** — the command and what it prints.
5. **Result** — the calibration produced.
6. **Validation** — the same quantities from an independent source, and the
   difference. This is the part that makes an example evidence rather than a
   demonstration.
7. **Reproducibility** — code version, config, and data hash needed to obtain
   the same numbers.

:::note[Plots are generated, not screenshotted]
Figures on these pages come out of the run they document. Re-running the
reproducibility block regenerates them — if a plot and the text disagree, the
plot is right.
:::

## Contributing an example

A run on a dataset we do not have is the most useful thing you can send. See
[Submit a dataset](/contribute/submit-dataset/) for the format, and
[Submit results](/contribute/submit-results/) if you want it on the
[leaderboard](/benchmark/leaderboard/) as well.
